# Caso 4 — NumPy: vectorización y embeddings

El benchmark loop-vs-vectorizado y el ejemplo estrella: similitud coseno
top-k sobre una matriz de embeddings — el corazón de un RAG en 4 líneas.

```bash
uv run --with numpy caso.py
```

Páginas: **NumPy** (Herramientas) y **Álgebra lineal** (Science).
