# Caso 2 — EDA con Polars

Los 5 pasos del EDA sobre un dataset sintético de ventas (se genera solo):
estructura, nulos/duplicados, distribuciones, outliers por z-score, correlación.

```bash
uv run --with polars caso.py     # o: pip install polars && python caso.py
```

Página: **EDA** (track Analytics).
