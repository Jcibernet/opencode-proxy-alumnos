# Caso 12 — ML clásico: churn con split honesto + segmentos con K-means

Supervisado (clasificador de churn con train/test y la trampa del accuracy
demostrada) y no supervisado (K-means sobre features normalizadas).

```bash
uv run --with scikit-learn --with numpy caso.py
```

Página: **ML supervisado y no supervisado** (Science).
