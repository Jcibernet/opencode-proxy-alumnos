# Caso 6 — Cálculo: descenso de gradiente desde cero

Regresión lineal entrenada a mano en 20 líneas: predicción → error →
gradiente → paso. La esencia de cómo entrena todo ML, sin frameworks.

```bash
uv run --with numpy caso.py
```

Página: **Cálculo y optimización** (Science).
