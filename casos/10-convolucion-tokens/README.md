# Caso 10 — Convolución y tokens, a mano

Las dos operaciones detrás de la IA moderna, sin frameworks: un detector de
bordes convolucional en NumPy y un mini-BPE que construye tokens por frecuencia.

```bash
uv run --with numpy caso.py
```

Página: **Redes convolucionales y tokens** (Science).
